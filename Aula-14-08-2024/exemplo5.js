const frutas = ['maçã', 'banana', 'laranja'];

frutas.forEach(function(fruta) {
    console.log(fruta)})

// tem que usar o const & forEach quando usado com uma function lista os elementos na vertical